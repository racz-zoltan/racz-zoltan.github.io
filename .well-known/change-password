CarryPass does not use account-based passwords or a server-side password reset flow.

This file exists only as a placeholder note for tools or humans checking `.well-known/change-password`.

There is no central CarryPass account password to change.
User secrets, generated passwords, local settings, and vault access are handled client-side.
